# Execution Backlog

| task_id | milestone | description | owner | status | depends_on | exit_evidence |
|---|---|---|---|---|---|---|
| `M1-001` | M1 | Finalize safe batch-sign nonce model (one-time nonces per hash/signing round). | core | done | none | Design note (`09-batch-sign-nonce-model.md`) + `bifrost-core::nonce::tests::outgoing_signing_nonces_are_single_use`. |
| `M1-002` | M1 | Implement batched signing in `bifrost-core::sign` with safe nonce handling. | core | done | `M1-001` | Added `create_partial_sig_packages_batch` and `combine_signatures_batch` (Option-B safe multiplexing), with batch nonce/session count guards and passing core batch tests. |
| `M1-003` | M1 | Expand signature-share verification negative tests (invalid share/member mismatch). | core | done | none | Added tests in core/node suites. |
| `M2-001` | M2 | Add strict wire payload bounds/shape validation equivalents from TS schema constraints. | codec | done | none | Reject-path tests in `bifrost-codec::rpc::tests` and `bifrost-codec::wire::tests`. |
| `M2-002` | M2 | Add explicit parse helpers parity map (`parse_session`, `parse_ecdh`, etc.). | codec/node | done | `M2-001` | Added `bifrost-codec::parse` helpers (`parse_session`, `parse_ecdh`, `parse_psig`, `parse_onboard_*`, `parse_ping`, `parse_group_package`, `parse_share_package`) with tests and node call-site integration. |
| `M3-001` | M3 | Enforce strict member binding and sender authorization checks in node handlers. | node | done | none | Node tests: sender/peer mismatch + onboard idx mismatch rejection. |
| `M3-002` | M3 | Add replay/idempotency protections for request IDs and stale envelopes. | node | done | `M3-001` | Added node replay cache + stale request-id checks (`request_ttl_secs`, `request_cache_limit`) and tests `handle_incoming_rejects_replayed_request_id` + `handle_incoming_rejects_stale_request_id`. |
| `M3-003` | M3 | Add payload limit enforcement equivalent to TS security flags. | node | done | none | Added node payload-limit checks (`max_request_id_len`, `max_sender_len`, `max_echo_len`, `max_sign_content_len`, batch caps) and reject-path tests for oversized echo/sign content. |
| `M4-001` | M4 | Add websocket reconnect/backoff and connection state transitions. | transport-ws | done | none | Added `ConnectionState` + `WsTransportConfig` (`max_retries`, exponential capped backoff), connect retry/backoff flow, and ws transport state/backoff tests. |
| `M4-002` | M4 | Add multi-relay strategy/failover and health tracking. | transport-ws | done | `M4-001` | Added relay health model (`RelayHealth`), health-ranked relay ordering for connect failover, active relay tracking, and relay-health unit tests. |
| `M4-003` | M4 | Stabilize cast/request threshold behavior under partial response loss. | transport-ws | done | `M4-001` | `cast` now runs peer requests concurrently with deterministic quorum-unreachable detection (`threshold_unreachable`) and transport tests for threshold decision logic. |
| `M5-001` | M5 | Implement sign/ecdh request batchers in node crate. | node | done | M1 batch design | Added queue batcher APIs `sign_queue` + `ecdh_batch` with chunked processing and tests (`sign_queue_processes_multiple_chunks`, `ecdh_batch_processes_multiple_chunks`). |
| `M5-002` | M5 | Implement ECDH cache (TTL/LRU) equivalent to TS behavior. | node | done | none | Added node-side TTL/LRU ECDH cache (`ecdh_cache_ttl_secs`, `ecdh_cache_max_entries`) with cache-hit/expiry tests (`ecdh_uses_cache_for_repeated_pubkey`, `ecdh_cache_entry_expires_after_ttl`). |
| `M5-003` | M5 | Add event model/emitter parity for lifecycle, message, bounced/info events. | node | done | none | Added node event subscription API (`subscribe_events`) and emissions for lifecycle/message/bounced/info/error paths with event tests (`connect_emits_ready_and_info_events`, `handle_incoming_emits_message_event`). |
| `M6-001` | M6 | Build cross-crate integration suite for onboard/ping/sign/ecdh happy paths. | qa | done | M1-M5 | Added cross-crate integration test `crates/bifrost-node/tests/happy_paths.rs` (`integration_happy_paths_echo_ping_onboard_sign_ecdh`). |
| `M6-002` | M6 | Build adversarial suite: tampered signatures, malformed sessions, nonce exhaustion. | qa | done | M1-M5 | Added adversarial integration suite `crates/bifrost-node/tests/adversarial.rs` covering tampered signature shares, malformed sessions, and nonce exhaustion. |
| `M7-001` | M7 | Update root/crate READMEs to reflect current and target parity status. | docs | done | M1-M6 | Updated root README and added/updated crate READMEs (`bifrost-core`, `bifrost-codec`, `bifrost-transport`, `bifrost-node`, `bifrost-transport-ws`) with current parity/test guidance. |
| `M7-002` | M7 | Provide runnable examples for node+ws multi-peer flow in `contrib/example`. | docs/qa | done | M4-M6 | Added `contrib/example` package with runnable dry-run and optional network connect mode plus README instructions. |
| `M8-001` | M8 | Add CI matrix for full workspace checks/tests including WS crate. | infra | done | M4-M6 | Added `.github/workflows/ci.yml` matrix for fmt/check/tests (core/node/ws/integration/example). |
| `M8-002` | M8 | Create migration guide from TS usage to Rust APIs. | docs | done | M5-M7 | Added `dev/artifacts/migration-guide-ts-to-rs.md`. |
| `M8-003` | M8 | Produce formal TS→RS gap report with release-priority ranking and execution order. | docs | done | M8-002 | Added `dev/artifacts/gap-report-ts-vs-rs.md` with parity metrics, blocker classification, and implementation roadmap. |
| `M8-004` | M8 | Create package audit kit (scope, checklist, command matrix, report template) and capture initial internal audit evidence. | security/release | done | M8-001,M8-003 | Added `dev/audit/AUDIT.md`, `dev/audit/checklist-v0.1.0.md`, and `dev/audit/internal-audit-2026-02-27.md`; executed `cargo check --workspace --offline`, core/ws/runtime test suites, and serial devnet e2e scripts. |
| `M8-005` | M8 | Implement canonical audit template framework with per-category markdown artifacts and run summary. | security/release | done | M8-004 | Added `dev/audit/templates/{00-index,01-architecture,02-completeness,03-separation-boundaries,04-security,05-technical-debt,06-code-smell,07-readability,08-documentation,09-testing-quality,10-reliability-operability,11-release-supply-chain,99-summary}.template.md`; updated `dev/audit/AUDIT.md`, `dev/audit/README.md`, `RELEASES.md`, and planner evidence links. |
| `M8-006` | M8 | Add uninterrupted multi-agent audit runbook with delegation, note-sharing, finding collection, and remediation tracking artifacts. | security/release | done | M8-005 | Added `dev/audit/RUNBOOK.md` plus templates (`00-index`, `99-summary`, `category`, `agent-brief`, `12-agent-assignments`, `13-shared-notes`, `14-findings-log`, `15-remediation-queue`); updated `dev/audit/AUDIT.md`, `dev/audit/README.md`, `README.md`, and planner evidence entries. |
| `M9-001` | M9 | Port TS demo relay to Rust as a reusable development relay crate/binary. | runtime | done | none | Added `bifrost-relay-dev` crate with REQ/EVENT/CLOSE flow, event cache/subscriptions/filtering, BIP-340 verify path, and `cargo test -p bifrost-relay-dev --offline` evidence. |
| `M9-002` | M9 | Add `bifrostd` daemon target with local socket RPC and node runtime loop. | runtime | done | `M9-001` | Added `bifrostd` crate with unix-socket JSON-RPC, config loader, inbound/event loop tasks, compile checks, and passing end-to-end smoke via `scripts/devnet.sh smoke` (relay + 3 daemons + CLI health/status/events). |
| `M9-003` | M9 | Add `bifrost-cli` target for agent/script automation against daemon RPC. | runtime | done | `M9-002` | Added `bifrost-cli` crate with health/status/events/echo/ping/onboard/sign/ecdh/shutdown commands and `cargo check -p bifrost-cli --offline`. |
| `M9-004` | M9 | Add `bifrost-tui` target wired to daemon event/status streams. | runtime | done | `M9-002` | Added `bifrost-tui` interactive shell over daemon RPC with command loop and `cargo check -p bifrost-tui --offline`. |
| `M9-005` | M9 | Upgrade `bifrost-tui` to a `ratatui`/`crossterm` MVP with pane-based UX. | runtime | done | `M9-004` | Replaced line shell with pane-based `ratatui`/`crossterm` app (status/events/output/input + command executor + periodic refresh), verified by `cargo check -p bifrost-tui --offline`. |
| `M9-006` | M9 | Add explicit end-to-end integration coverage per RPC method in `bifrost-node` (`echo`, `ping`, `onboard`, `sign`, `ecdh`). | qa/runtime | done | `M9-002` | Node integration test suite includes per-method e2e tests and passes via `cargo test -p bifrost-node --test happy_paths --offline`. |
| `M9-007` | M9 | Add `bifrost-cli` end-to-end integration test suite against a mock Unix-socket RPC daemon. | qa/runtime | done | `M9-003` | CLI integration tests validate command->RPC mapping and output behavior across all RPC methods via `cargo test -p bifrost-cli --test e2e_cli --offline`. |
| `M9-008` | M9 | Add devnet-backed TUI e2e automation that exercises all RPC methods against real nodes. | qa/runtime | done | `M9-005` | `scripts/test-tui-e2e.sh` runs `bifrost-tui` scripted mode against real devnet daemons and executes `health/status/events/ping/echo/onboard/sign/ecdh` with asserted outputs. |
| `M9-009` | M9 | Add a `bifrost-node` devnet e2e script covering keyset generation/distribution, onboarding, and all RPC methods. | qa/runtime | done | `M9-002` | Added `scripts/test-node-e2e.sh`; pass evidence on real devnet (`node e2e passed`) including key material/distribution checks plus `ping/echo/onboard/sign/ecdh` execution assertions. |
| `M9-010` | M9 | Fix TUI operator UX defects: reliable `ping`/`echo` command usability, auto-following event/output panes, and useful peer nonce-pool status panel. | runtime/tui | done | `M9-005` | Added peer selector resolution (`index`, alias, pubkey-prefix), tail-follow rendering for status/events/output, daemon status nonce-pool telemetry, and `bifrost-tui` unit coverage; verified with `cargo check -p bifrost-core -p bifrost-node -p bifrost-rpc -p bifrostd -p bifrost-tui --offline`, `cargo test -p bifrost-tui --offline`, and `scripts/test-tui-e2e.sh` (`tui e2e passed`). |
| `M9-011` | M9 | Improve TUI peer UX: explicit alias targeting (`alice,bob,carol`) and per-peer nonce health progress bars (`theirs/ours`) in status panel. | runtime/tui | done | `M9-010` | Added daemon status nonce-pool thresholds (`nonce_pool_size`, min/critical), alias-targetable peer resolution (`alice,bob,carol,index,prefix`), and status progress-bar rendering per peer (`theirs/ours`); verified by `cargo check -p bifrost-rpc -p bifrostd -p bifrost-tui --offline`, `cargo test -p bifrost-tui --offline`, and `scripts/test-tui-e2e.sh` (`tui e2e passed`). |
| `M10-001` | M10 | Replace raw JSON websocket envelopes with Nostr-native `REQ`/`EVENT`/`CLOSE` transport framing and request correlation. | transport-ws | done | `M9-001`,`M9-002` | Reworked `bifrost-transport-ws` to publish/subscribe via Nostr frames (kind-filtered `REQ`, signed `EVENT`, `CLOSE`) with RPC correlation; verified by `cargo test -p bifrost-transport-ws --offline`, `scripts/test-node-e2e.sh`, and `scripts/test-tui-e2e.sh`. |
| `M10-002` | M10 | Add encrypted RPC envelope wrapper for peer-targeted event payloads (NIP-44 target, hardening follow-up permitted). | transport-ws | done | `M10-001` | Implemented NIP-44-v2-compatible payload processing in transport (`version=2`, conversation key HKDF extract, per-message HKDF expand, NIP-44 padding, ChaCha20 stream cipher, HMAC-SHA256 AAD over nonce+ciphertext, base64url-no-pad encoding) with sender-tag binding checks; verified by `cargo test -p bifrost-transport-ws --offline`, `scripts/test-node-e2e.sh`, and `scripts/test-tui-e2e.sh`. |
| `M10-003` | M10 | Update daemon/devnet config schema for explicit Nostr transport options (`rpc_kind`, sender keys, timeouts). | runtime | done | `M10-001` | Added daemon `transport` config (`rpc_kind`, retry/backoff, sender keys) plus `bifrost-devnet` config generation; validated with `cargo check -p bifrost-devnet -p bifrostd --offline` and passing node/tui e2e scripts. |
| `M10-004` | M10 | Tighten e2e scripts to require successful ping/echo in healthy devnet (remove timeout-as-success fallbacks). | qa/runtime | done | `M10-001` | Updated `scripts/test-node-e2e.sh` and `scripts/test-tui-e2e.sh` to require success-path outputs for all RPC methods (`ping/echo/onboard/sign/ecdh`); verified both scripts pass. |
| `M10-005` | M10 | Expand NIP-44 compatibility coverage with deterministic multi-length matrix and negative mutation cases. | transport-ws | done | `M10-002` | Added `nip44_deterministic_matrix_and_mutation_rejects` plus expanded fixture corpus coverage (`wrong-key`, `invalid base64`, `invalid length`, `unknown version`, deterministic/random roundtrip) and verified with `cargo test -p bifrost-transport-ws --offline`, `scripts/test-node-e2e.sh`, `scripts/test-tui-e2e.sh`. |

## Execution Pass 1 (PR-Sized Plan)

| pr_id | scope | backlog_tasks | planned changes | acceptance checks |
|---|---|---|---|---|
| `PR-01` | Batch-sign nonce design and guardrails | `M1-001` | Finalize nonce lifecycle design in `09-batch-sign-nonce-model.md` and introduce explicit API invariants in `bifrost-core` docs/errors. | New/updated tests prove nonce single-use and reject reuse paths. |
| `PR-02` | Codec validation hardening | `M2-001` | Add strict bounds/shape checks for wire payloads and reject malformed envelopes early. | Codec tests for invalid payload shapes and size limits pass. |
| `PR-03` | Sender/member binding security | `M3-001` | Enforce sender-to-member checks in sign/ecdh/ping handler paths and reject unauthorized messages. | Node tests demonstrate unauthorized sender rejection and valid sender acceptance. |
| `PR-04` | Batch-sign implementation | `M1-002` | Implement safe multi-hash signing flow after `PR-01` design is locked. | Core/node batch signing tests pass; parity matrix updated. |
| `PR-05` | Signature-share adversarial tests | `M1-003` | Add invalid share/member mismatch/tamper test matrix in core and node layers. | Negative-path tests pass and evidence linked in `06-test-strategy.md`. |
